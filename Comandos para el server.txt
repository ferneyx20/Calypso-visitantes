
npm install
npm install prisma
npx prisma generate
Remove-Item -Recurse -Force prisma\migrations  remover migración
npx prisma migrate dev --name init_db insertar estructura de base datos
rm -rf prisma/migrations remover migración forsada
npx prisma migrate reset insertar nuevos datos reseteando base
npx prisma db seed insertar nuevos datos sin borrar
npm install bcryptjs hasheo de contraseñas
npm install -D @types/bcryptjs 
npm install -D typescript ts-node @types/node @types/bcryptjs
npm install papaparse insertar en csv
npm install -D @types/papaparse

npm install jsonwebtoken
npm install -D @types/jsonwebtoken

npm run dev iniciar servidor 
 

cedula:
1007430584	PULIDO	PULIDO	JHOAN	FERNEY	M	22602000	B+

1007430584	PULIDO	PULIDO	JHOAN	FERNEY	M	22602000	B+





POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin123
POSTGRES_DB=BD_visitantes
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

explico problema.

Indico la estructura de carpetas en archivo txt me indicas que archivos modificar te doy el codigo completo 
del archivo devuelve archivo con codigo completo modificado archivo por archivo 

explicando brevemente los cambios para no perderme con tanta informacion